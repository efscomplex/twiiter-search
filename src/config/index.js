export const options = {
	lang: 'es',
	maxResults: 10,
	nextToken: ''
}
export const query = 'hulk'
export const scrollOpts = {
	targetId: 'lastFeed'
}
export const theme = {
	light: {
		fg: 'var(--dark)',
		bg: 'white'
	},
	dark: {
		bg: 'var(--dark)',
		fg: 'var(--light)'
	}
}
